# Next AI: start here

1. Verify `gh auth status` and authenticate the intended GitHub account.
2. Inspect `git status -sb` in all three implementation repositories; preserve unrelated changes.
3. On the Redmi, finish the student Google password check and confirm the OAuth callback returns to `com.example.folk_app.test` with a valid Supabase session.
4. Submit one synthetic daily sadhana record and verify it persists.
5. Sign into the guide test app and verify only the assigned guide/HOD can see the synthetic record.
6. Commit isolated debug-package changes to their existing `codex/overnight-foundation` branches if clean and approved.
7. Implement migrations/RLS and fixtures from `docs/DATA_MODEL.md` incrementally.
8. Update status and backlog before handing off.

Do not start UI expansion until authentication and the student-to-guide data loop are proven.

